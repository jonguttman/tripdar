Meaning Primitive
Schema Representation
Context
strainId, doseCategory, scale
Dimension
dimensionId (FK, constrained enum)
Reference
referenceFrame (enum)
Direction
direction (enum)
Signal
Immutable row
Story
Separate table (later, optional)
⚠️ If you can’t point to a line in Meaning Layer v1.0, it doesn’t get a column.